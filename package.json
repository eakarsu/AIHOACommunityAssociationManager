{
  "name": "ai-hoa-community-association-manager",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "cd server && npm install && cd ../client && npm install",
    "dev": "concurrently \"cd server && npm run dev\" \"cd client && npm run dev\"",
    "start": "bash start.sh"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
